const Header = () => {
  return (
    <header>
      <div className="logo">RESTAURANT</div>
      <nav>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Products</a>
        <a href="#">Contacts</a>
      </nav>
    </header>
  );
};

export default Header;
